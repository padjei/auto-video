import {execFileSync} from 'node:child_process';
const checks:[string,string,string[]][]=[['Node','node',['--version']],['npm','npm',['--version']],['FFmpeg','ffmpeg',['-version']],['FFprobe','ffprobe',['-version']]];let failed=false;for(const [label,cmd,args] of checks){try{const out=execFileSync(cmd,args,{encoding:'utf8',stdio:['ignore','pipe','pipe']});console.log(`✓ ${label}: ${out.split('\n')[0]}`);}catch{console.error(`✗ ${label}: not available`);failed=true;}}if(failed){console.error('Install missing prerequisites before rendering.');process.exit(1);}console.log('Environment looks ready.');
